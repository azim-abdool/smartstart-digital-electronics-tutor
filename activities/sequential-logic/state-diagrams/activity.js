"use strict";
let state=0,EN=0;const names=['A','B','C','D'];function render(){const b=document.querySelector('#en');b.textContent=EN;b.classList.toggle('on',EN===1);document.querySelectorAll('.state-node').forEach(n=>n.classList.toggle('active',+n.dataset.state===state));document.querySelectorAll('#transition-table tbody tr').forEach(r=>r.classList.toggle('highlight',+r.dataset.s===state));document.querySelector('#state-status').textContent=`Current state ${names[state]} represents Q₁Q₀=${state.toString(2).padStart(2,'0')}. With EN=${EN}, the next state is ${names[EN?(state+1)%4:state]}.`;}
document.querySelector('#en').addEventListener('click',()=>{EN^=1;render();});document.querySelector('#step').addEventListener('click',()=>{if(EN)state=(state+1)%4;render();});render();document.querySelector('#check').addEventListener('click',()=>{const v=document.querySelector('input[name=answer]:checked')?.value,ok=v==='D',f=document.querySelector('#feedback');f.className='feedback '+(ok?'correct':'incorrect');f.textContent=ok?'Correct. C represents 10; with EN=1 the counter advances to 11, state D.':'Not correct. Follow the EN=1 column from state C.';if(ok)SmartStartProgress.complete('state-diagrams');});
