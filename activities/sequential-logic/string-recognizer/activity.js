"use strict";
let bits=[],locked=false;const stateMap={'':'RESET','0':'S1','01':'S2','010':'S3','1':'S4','10':'S5','LOCK':'S6'};function suffixState(){if(locked)return 'S6';const s=bits.join('');if(s.endsWith('010'))return 'S3';if(s.endsWith('01'))return 'S2';if(s.endsWith('10'))return 'S5';if(s.endsWith('0'))return 'S1';if(s.endsWith('1'))return 'S4';return 'RESET';}function enter(bit){bits.push(bit);const last3=bits.slice(-3).join('');let z=0;if(last3==='100')locked=true;if(last3==='010'&&!locked)z=1;render(z,bit,last3);}function render(z=0,bit=null,last3=bits.slice(-3).join('').padStart(3,'0')){document.querySelector('#stream').innerHTML=bits.length?bits.map((b,i)=>`<span class="stream-bit${locked&&i>=bits.length-3?' danger':''}">${b}</span>`).join(''):'<span>Input stream is empty.</span>';document.querySelector('#state').textContent=suffixState();document.querySelector('#window').textContent=last3.padStart(3,'0');document.querySelector('#output').textContent=z;document.querySelector('#recognizer-status').className='feedback '+(z?'correct':locked?'incorrect':'');document.querySelector('#recognizer-status').textContent=locked?'The sequence 100 has occurred. State S6 is locked; Z remains 0 until RESET.':z?'Detected 010. Z is asserted for this input step.':bit===null?'Start in RESET and enter bits.':`Accepted ${bit}. The retained suffix corresponds to ${suffixState()}.`;}
document.querySelector('#input-0').addEventListener('click',()=>enter(0));document.querySelector('#input-1').addEventListener('click',()=>enter(1));document.querySelector('#reset').addEventListener('click',()=>{bits=[];locked=false;render();});render();document.querySelector('#check').addEventListener('click',()=>{const v=document.querySelector('input[name=answer]:checked')?.value,ok=v==='assert',f=document.querySelector('#feedback');f.className='feedback '+(ok?'correct':'incorrect');f.textContent=ok?'Correct. The latest three bits are 010 and the lockout sequence 100 has not occurred, so Z=1.':'Not correct. The required pattern is exactly 010, and no lockout has yet occurred.';if(ok)SmartStartProgress.complete('string-recognizer');});
