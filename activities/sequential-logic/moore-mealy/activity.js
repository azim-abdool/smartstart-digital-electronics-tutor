"use strict";
let X=0;const input=document.querySelector('#input');function render(){input.textContent=X;input.classList.toggle('on',X===1);const s=document.querySelector('#state').value;const moore=s==='B'?1:0;const mealy=(s==='B'&&X===1)?1:0;const next=X?(s==='A'?'B':'A'):s;document.querySelector('#moore-output').textContent=moore;document.querySelector('#mealy-output').textContent=mealy;document.querySelector('#next-state').textContent=next;document.querySelector('#response').textContent=`In state ${s}, changing X ${X?'to 1':'to 0'} ${mealy!==moore?'changes the Mealy output independently of the Moore output.':'currently gives the same numeric output, but the dependencies remain different.'}`;}
input.addEventListener('click',()=>{X^=1;render();});document.querySelector('#state').addEventListener('change',render);render();document.querySelector('#check').addEventListener('click',()=>{const v=document.querySelector('input[name=answer]:checked')?.value,ok=v==='mealy',f=document.querySelector('#feedback');f.className='feedback '+(ok?'correct':'incorrect');f.textContent=ok?'Correct. MAX includes EN, so the output depends on both state and input: a Mealy output.':'Not correct. A Moore output cannot depend directly on an external input.';if(ok)SmartStartProgress.complete('moore-mealy');});
