"use strict";
function path(amplitude){let d='M0 120';for(let x=0;x<=760;x+=10){const spike=(x%120<20?1:0),y=120+Math.sin(x/24)*amplitude+(spike?Math.sin(x*1.7)*amplitude*1.7:0);d+=` L${x} ${y.toFixed(1)}`;}return d;}const raw=document.querySelector('#raw-wave'),clean=document.querySelector('#clean-wave'),grid=document.querySelector('#grid');raw.setAttribute('d',path(18));clean.setAttribute('d',path(4));let lines='';for(let x=0;x<=760;x+=40)lines+=`<line class="wave-grid" x1="${x}" y1="20" x2="${x}" y2="220"/>`;for(let y=40;y<=200;y+=40)lines+=`<line class="wave-grid" x1="0" y1="${y}" x2="760" y2="${y}"/>`;grid.innerHTML=lines;let cap=false;document.querySelector('#capacitor-toggle').addEventListener('click',e=>{cap=!cap;e.target.textContent=`Decoupling capacitor: ${cap?'ON':'OFF'}`;if(cap)clean.removeAttribute('hidden');else clean.setAttribute('hidden','');raw.style.opacity=cap?'.25':'1';const n=document.querySelector('#decoupling-note');n.className='feedback '+(cap?'correct':'incorrect');n.textContent=cap?'The local capacitor supplies and absorbs short switching currents, reducing the disturbance at the IC supply pin.':'Without local charge storage, switching spikes and dips reach the supply pin more strongly.';});
let stage=0;const board=document.querySelector('#breadboard'),next=document.querySelector('#next-stage'),note=document.querySelector('#stage-note');function render(){board.className='breadboard '+(stage?`stage-${stage}`:'');const texts=['Start by placing the IC across the breadboard’s centre gap.','IC placed across the centre gap.','Power and ground connected using the distribution rails.','Decoupling capacitor placed close to the IC between VCC and ground.'];note.textContent=texts[stage];next.textContent=stage===0?'Place IC':stage===1?'Connect power and ground':stage===2?'Add decoupling capacitor':'Sequence complete';next.disabled=stage===3;}next.addEventListener('click',()=>{stage=Math.min(3,stage+1);render();});document.querySelector('#reset-stage').addEventListener('click',()=>{stage=0;render();});render();
document.querySelector('#check-list').addEventListener('click',()=>{const all=[...document.querySelectorAll('.checklist input')].every(x=>x.checked),f=document.querySelector('#feedback');f.className='feedback '+(all?'correct':'incorrect');f.textContent=all?'Prepared. The original six breadboard reminders are all acknowledged.':'Mark every item, including floating-input treatment and decoupling.';if(all)SmartStartProgress.complete('hw-decoupling-breadboard');});