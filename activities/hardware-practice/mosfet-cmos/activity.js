"use strict";
const type=document.querySelector('#mos-type'),gate=document.querySelector('#gate-level'),transistor=document.querySelector('#transistor');
function mos(){const t=type.value,g=Number(gate.value),on=t==='n'?g===1:g===0;transistor.classList.toggle('on',on);document.querySelector('#mos-state').textContent=on?'ON / low resistance':'OFF / high resistance';document.querySelector('#mos-explanation').textContent=t==='n'?`An n-channel device is ${on?'enabled by a HIGH gate level':'disabled by a LOW gate level'}.`:`A p-channel device is ${on?'enabled by a LOW gate level':'disabled by a HIGH gate level'}.`;}
[type,gate].forEach(e=>e.addEventListener('change',mos));mos();
let inv=0;const invBtn=document.querySelector('#inverter-input');function inverter(){const y=1-inv;invBtn.textContent=`A = ${inv}`;document.querySelector('#inverter-output').textContent=`Y = ${y}`;document.querySelector('#p-state').textContent=inv===0?'ON':'OFF';document.querySelector('#n-state').textContent=inv===1?'ON':'OFF';document.querySelector('#inverter-path').textContent=inv===0?'Pull-up path connects output to VCC.':'Pull-down path connects output to ground.';}invBtn.addEventListener('click',()=>{inv=1-inv;inverter();});inverter();
let a=0,b=0;const gateType=document.querySelector('#gate-type'),aBtn=document.querySelector('#input-a'),bBtn=document.querySelector('#input-b');function logic(){const kind=gateType.value,y=kind==='nand'?Number(!(a&&b)):Number(!(a||b));aBtn.textContent=`A = ${a}`;bBtn.textContent=`B = ${b}`;document.querySelector('#gate-output').textContent=`Y = ${y}`;document.querySelector('#network-detail').textContent=kind==='nand'?'n-channel pull-down devices are in series; p-channel pull-up devices are in parallel.':'n-channel pull-down devices are in parallel; p-channel pull-up devices are in series.';}aBtn.addEventListener('click',()=>{a=1-a;logic();});bBtn.addEventListener('click',()=>{b=1-b;logic();});gateType.addEventListener('change',logic);logic();
document.querySelector('#check-answer').addEventListener('click',()=>{const ok=document.querySelector('input[name=check]:checked')?.value==='b',f=document.querySelector('#feedback');f.className='feedback '+(ok?'correct':'incorrect');f.textContent=ok?'Correct. CMOS uses complementary n-channel and p-channel switching networks.':'Not correct. Focus on the two complementary MOSFET types and their opposing conduction conditions.';if(ok)SmartStartProgress.complete('hw-mosfet-cmos');});