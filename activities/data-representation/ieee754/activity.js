"use strict";
function float32Bits(value){const buf=new ArrayBuffer(4),view=new DataView(buf);view.setFloat32(0,value,false);const u=view.getUint32(0,false);return u.toString(2).padStart(32,"0");}
function render(){const v=Number(document.querySelector("#float-input").value);if(!Number.isFinite(v)){document.querySelector("#decoded-expression").textContent="Enter a finite value";return;}const bits=float32Bits(v),s=bits[0],e=bits.slice(1,9),f=bits.slice(9),ei=parseInt(e,2),hex=parseInt(bits,2).toString(16).toUpperCase().padStart(8,"0");document.querySelector("#hex-word").textContent=`0x${hex}`;document.querySelector("#bit-fields").innerHTML=[...bits].map((b,i)=>`<span class="bit ${i===0?"sign":i<9?"exponent":"fraction"}">${b}</span>`).join("");document.querySelector("#sign-field").textContent=s;document.querySelector("#exponent-field").textContent=`${e} (${ei})`;let text;if(ei===0)text="Subnormal or zero";else if(ei===255)text="Infinity or NaN";else{text=String(ei-127);}document.querySelector("#unbiased-field").textContent=text;const sign=s==="1"?"−":"+";const trimmed=f.replace(/0+$/g,"")||"0";document.querySelector("#decoded-expression").innerHTML=(ei>0&&ei<255)?`${sign}1.${trimmed} × 2<sup>${ei-127}</sup>`:(ei===0?`${sign}0.${trimmed} × 2<sup>−126</sup>`:"Special IEEE 754 value");}
const input=document.querySelector("#float-input");input.addEventListener("input",render);document.querySelector("#original-example").addEventListener("click",()=>{input.value=201;render();});render();
document.querySelector("#ieee-check").addEventListener("click",()=>{const ok=document.querySelector("#exponent-answer").value==="10000110"&&document.querySelector("#fraction-answer").value==="10010010000000000000000",fb=document.querySelector("#ieee-feedback");if(ok){fb.className="feedback correct";fb.textContent="Correct. The sign is 0, the biased exponent is 127 + 7 = 134 = 10000110₂, and the leading 1 is implicit.";SmartStartProgress.complete("ieee754");}else{fb.className="feedback incorrect";fb.textContent="Not yet. Add the single-precision bias 127 to exponent 7, and omit the leading 1 from the stored fraction field.";}});
