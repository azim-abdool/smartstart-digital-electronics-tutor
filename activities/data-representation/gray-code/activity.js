"use strict";
function gray(n){return n^(n>>1);}function renderTable(){const w=Number(document.querySelector("#gray-bits").value),rows=[];for(let i=0;i<2**w;i++){const b=i.toString(2).padStart(w,"0"),g=gray(i).toString(2).padStart(w,"0"),prev=i?gray(i-1).toString(2).padStart(w,"0"):"—";let changed="—";if(i){const idx=[...g].findIndex((x,j)=>x!==prev[j]);changed=`bit ${w-1-idx}`;}rows.push(`<tr><td>${i}</td><td class="code-display">${b}</td><td class="code-display">${g}</td><td>${changed}</td></tr>`);}document.querySelector("#gray-rows").innerHTML=rows.join("");}document.querySelector("#gray-bits").addEventListener("change",renderTable);renderTable();
const seq=["000","001","011","010","110","111","101","100"];let pos=0;const visited=new Set([0]);const labels=document.querySelector("#sector-labels");labels.innerHTML=seq.map((code,i)=>{const a=(i*45-90)*Math.PI/180,x=150+112*Math.cos(a),y=150+112*Math.sin(a);return `<text x="${x}" y="${y}" text-anchor="middle" dominant-baseline="middle" font-size="14" font-weight="800" fill="#17324d">${code}</text>`;}).join("");
function renderRotary(){const code=seq[pos];document.querySelector("#rotary-position").textContent=pos;document.querySelector("#rotary-code").textContent=code;document.querySelector("#rotary-pointer").style.transform=`rotate(${pos*45}deg)`;document.querySelector("#sensor-row").innerHTML=[...code].map((b,i)=>`<div class="sensor ${b==="1"?"on":""}">S${2-i}<br>${b}</div>`).join("");const fb=document.querySelector("#rotary-feedback");if(visited.size===8){fb.className="feedback correct";fb.textContent="All eight positions visited. Each transition in the cyclic sequence changes exactly one sensor bit.";SmartStartProgress.complete("gray-code");}else{fb.className="feedback";fb.textContent=`${visited.size} of 8 positions visited. The next Gray code differs from the current code in one bit.`;}}
document.querySelector("#rotate-button").addEventListener("click",()=>{pos=(pos+1)%8;visited.add(pos);renderRotary();});document.querySelector("#reset-rotary").addEventListener("click",()=>{pos=0;visited.clear();visited.add(0);renderRotary();});renderRotary();
