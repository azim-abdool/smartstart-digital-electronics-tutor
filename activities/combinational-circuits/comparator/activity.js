"use strict";
function render(){const a=+document.querySelector('#a').value,b=+document.querySelector('#b').value;document.querySelector('#a-value').textContent=a;document.querySelector('#b-value').textContent=b;document.querySelector('#a-bin').textContent=a.toString(2).padStart(4,'0');document.querySelector('#b-bin').textContent=b.toString(2).padStart(4,'0');[['lt',a<b],['eq',a===b],['gt',a>b]].forEach(([id,on])=>{const e=document.querySelector('#'+id);e.classList.toggle('on',on);e.querySelector('span').textContent=on?1:0;});}
document.querySelector('#a').addEventListener('input',render);document.querySelector('#b').addEventListener('input',render);render();document.querySelector('#check').addEventListener('click',()=>{const v=document.querySelector('input[name=relation]:checked')?.value,ok=v==='gt',fb=document.querySelector('#feedback');fb.className='feedback '+(ok?'correct':'incorrect');fb.textContent=ok?'Correct. 1011₂ is 11 and 0111₂ is 7, so A>B.':'Not correct. Convert or compare the words from the MSB.';if(ok)SmartStartProgress.complete('comparator');});
