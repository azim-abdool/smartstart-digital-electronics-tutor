"use strict";
function bit(id,render){const b=document.querySelector(id);b.classList.toggle('on',b.textContent==='1');b.addEventListener('click',()=>{b.textContent=b.textContent==='0'?'1':'0';b.classList.toggle('on',b.textContent==='1');render();});}
function render(){const en=+document.querySelector('#en').textContent,s1=+document.querySelector('#s1').textContent,s0=+document.querySelector('#s0').textContent,w=+document.querySelector('#w').textContent,index=s1*2+s0;document.querySelector('#outputs').innerHTML=[0,1,2,3].map(i=>`<div class="route-output ${en&&i===index?'active':''}"><strong>Y${i}</strong><div>${en&&i===index?w:0}</div></div>`).join('');const note=document.querySelector('#note');note.textContent=en?`W is routed to Y${index}; the other outputs are 0.`:'The device is disabled, so all outputs are 0.';}
['#en','#s1','#s0','#w'].forEach(id=>bit(id,render));render();document.querySelector('#check').addEventListener('click',()=>{const ok=document.querySelector('input[name=demuxq]:checked')?.value==='Y2',fb=document.querySelector('#feedback');fb.className='feedback '+(ok?'correct':'incorrect');fb.textContent=ok?'Correct: select code 10 routes W to Y2.':'Try again: select code 10₂ is output number 2.';if(ok)SmartStartProgress.complete('demultiplexer');});
